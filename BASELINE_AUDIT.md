# BASELINE_AUDIT — خطّ الأساس قبل تنفيذ خطة الإغلاق (المرحلة 0)

> 📌 **لقطة تاريخية (Phase 0 — 2026‑07‑01).** الأرقام أدناه (97 اختبارًا · رأس الترحيلات 0027) تعكس **حالة البداية** قبل العمل، وتُحفَظ كما هي للصدق التوثيقيّ ولا تُحدَّث. **للحالة الحالية** انظر: `plan.md` (رأس الحالة + سلسلة التحصين 1–13)، و`TESTING.md` (208 اختبارًا)، و`E2E_TEST_REPORT.md`، و`FINAL_RELEASE_AUDIT.md`. أحدث ترحيل الآن: **0038**.

**التاريخ:** 2026‑07‑01 · **الفرع:** `fix/R-0-baseline` · **رأس الترحيلات:** `0027`
**الغرض:** تثبيت حالة خضراء موثّقة قبل أي تعديل، ومطابقة التوثيق بالواقع، وحصر موانع الإطلاق.
**قاعدة:** لا تعديل ميزة قبل اعتماد هذا التقرير.

---

## 1) حالة Backend الحالية (مؤكَّدة بالتشغيل)

| الفحص | النتيجة |
|---|---|
| `python manage.py check` | ✅ 0 مشاكل |
| `python manage.py makemigrations --check --dry-run` | ✅ لا ترحيلات معلّقة |
| `python manage.py test api` | ✅ **97 اختبارًا تمرّ** (OK) |
| `python manage.py check --deploy` | ⚠️ 6 تحذيرات إنتاج متوقّعة عند التشغيل بلا متغيّرات بيئة الإنتاج: `W004 (HSTS)`, `W008 (SSL redirect)`, `W009 (SECRET_KEY)`, `W012 (SESSION_COOKIE_SECURE)`, `W016 (CSRF_COOKIE_SECURE)`, `W018 (DEBUG)` — تزول بضبط `DEPLOY.md`. |

**البنية:** Django 5.1 + DRF + SimpleJWT · نماذج غنية (فندق/غرف/حجوزات/مدفوعات/مصاريف/فوليو/طعام/عمولات/اشتراكات/تدقيق/إغلاق يومي/اتفاقية/2FA) · 27 ترحيلًا مطبَّقًا · عزل مستأجرين + RBAC + Audit Log.

## 2) حالة Frontend الحالية (مؤكَّدة بالتشغيل)

| الفحص | النتيجة |
|---|---|
| `npm run lint` | ✅ exit 0 (صفر مشاكل) |
| `npm run typecheck` (`tsc --noEmit`) | ✅ نظيف |
| `npm run build` | ✅ ناجح |
| `npm audit` | ⚠️ **2 ثغرة متوسطة** (moderate) — تحتاج معالجة |

**البنية:** Next.js 16 + React 19 · 4 مناطق (موقع عام / مدير / منصّة / استقبال) · i18n مشترك (`src/lib/i18n`) بمبدّل AR/EN للّوحات.

## 3) أوامر التشغيل والفحص

```bash
# تشغيل محلي
cd backend && python manage.py migrate && python manage.py runserver
cd frontend && npm install && npm run dev
# بوّابة الفحص الموحّدة (بعد كل مرحلة)
cd backend  && python manage.py check && python manage.py makemigrations --check --dry-run && python manage.py test api
cd frontend && npm run lint && npm run typecheck && npm run build
```

## 4) مطابقة التوثيق بالواقع (صدق التوثيق)

| ادّعاء في التوثيق | الواقع | الحكم |
|---|---|---|
| `plan.md`: «97 اختبارًا» | 97 تمرّ فعلًا | ✅ صحيح |
| `plan.md`: B‑3 (الجلسة) «مغلق ✅» | `authFetch` مستخدَم في **7** مواضع مقابل **186** `fetch` خام | ❌ مبالغ — التغطية ~4% |
| `plan.md`: M‑1 «i18n موحّد كامل ✅» | الموقع العام (`app/page.tsx`) لا يستورد `useLang` — عربي 100% | ❌ مبالغ — اللوحات فقط ثنائية اللغة |
| `plan.md` §5: `RELEASE_CHECKLIST.md` مطلوب | **مفقود** | ❌ مرجع معلّق في `DEPLOY.md` |
| `plan.md` §5: `SECURITY.md` مطلوب | **مفقود** | ❌ |
| `plan.md` §5: `frontend/.env.example` | **موجود** ✓ | ✅ (ادّعاء غيابه في مراجعة خارجية غير دقيق) |
| قاعدة سلسلة المال: «لا حذف مالي — إبطال + Audit» | `PaymentViewSet.perform_destroy` يحذف فعليًا **بلا** `record_audit` | ❌ **خرق للقاعدة** |

## 5) المشاكل المؤكَّدة (بدليل ملف:سطر)

| # | المشكلة | الدليل | الخطورة |
|---|---|---|---|
| C1 | **حذف الدفعة نهائيًا بلا تدقيق** (لا void، لا audit) | `views.py` `PaymentViewSet.perform_destroy` | 🔴 مانع |
| C2 | **الجلسة:** 186 `fetch` خام مقابل 7 `authFetch` + `.catch(()=>{})` صامت | `app/**/*.tsx` | 🔴 مانع |
| C3 | **ظهور الفنادق العامة** غير مصفّى بالحالة/الاشتراك (فقط `public_listing_enabled`) | `PublicHotelListView` | 🟠 عالٍ |
| C4 | **التقييمات تُنشَر تلقائيًا** (`is_approved default=True`) | `models.py:679` | 🟠 عالٍ |
| C5 | **الخط `Tajawal` عبر `next/font/google`** (اعتماد شبكة وقت البناء) | `app/layout.tsx:2` | 🟠 عالٍ (يفشل في بناء معزول) |
| C6 | **الصلاحيات الدقيقة غير مُلزَمة خادميًا** (تُعرَض للواجهة فقط) | `permissions.py` | 🟠 عالٍ |
| C7 | **دَين تصميم:** 2390 كتلة inline‑style + ملفات ضخمة (`reservations` 1643، `guests` 1628، `food-services` 1029) | `src/app` | 🟡 متوسط |
| C8 | **الموقع العام أحادي اللغة** بلا مبدّل | `app/page.tsx` (0 `useLang`) | 🟠 عالٍ (حسب السوق) |
| C9 | **ملفات توثيق مفقودة** + ترويسات ✅ لا تطابق الواقع | §4 أعلاه | 🟠 عالٍ |
| C10 | **2 ثغرة npm متوسطة** | `npm audit` | 🟡 متوسط |

## 6) المشاكل المشكوك بها (تحتاج تأكيدًا في مرحلتها)

- **Pagination:** نقاط مالك المنصّة تُرجع كل الصفوف (خطر أداء عند التوسّع) — يُؤكَّد في مرحلة الجاهزية.
- **تطبيع الهاتف:** إدارة/إلغاء الحجز العام بمطابقة حرفية — قد يمنع الزبون من إيجاد حجزه.
- **احتياطي الدور حسب اسم المستخدم** في `permissions.py` — خطر صلاحية إن غاب `UserProfile`.
- **سياسة تخزين الوثائق الحساسة** (base64 في الحقول) — تحتاج مراجعة إنتاجية.

## 7) قائمة موانع الإطلاق (Blockers) — موحّدة من 3 مراجعات + تحقّق مباشر

1. 🔴 **C1** حذف مالي بلا تدقيق → إبطال (void) + سبب + Audit.
2. 🔴 **C2** موثوقية الجلسة → طبقة `authFetch`/services + رسالة انتهاء جلسة.
3. 🟠 **C5** البناء المعزول → `next/font/local`.
4. 🟠 **C3/C4/C6** تحصين التعرّض العام: تصفية ظهور الفنادق + مراجعة التقييمات + إلزام الصلاحيات.
5. 🟠 **C8/C9** حسم لغة الموقع العام + صدق التوثيق (إنشاء الملفات + مطابقة ✅).

## 8) ترتيب مراحل التنفيذ المعتمد (خطة المالك، 0→15)

`0` خط أساس (هذا) · `1` إصلاح البناء (الخط) · `2` ظهور الفنادق · `3` تحصين الحجز العام · `4` توحيد طبقة API (authFetch/services) · `5` تقوية الأمان · `6` حماية العمليات المالية (void) · `7` الاتفاقية القانونية · `8` بروفايل وإعدادات الفندق · `9` المركزية والتصميم · `10` توحيد الترجمة · `11` تحسين الموقع العام · `12` اختبارات E2E · `13` جاهزية النشر · `14` تنظيف التوثيق · `15` التقييم النهائي.

**ملاحظة:** كل مرحلة مستقلّة، تُغلق ببوّابة خضراء + سطر Changelog صادق، ولا خلط بين المراحل.

---

**اعتماد المرحلة 0:** خطّ الأساس أخضر وموثَّق (97 اختبار + بناء ناجح)، والفجوات محصورة أعلاه. **جاهز للانتقال إلى المرحلة 1** بعد إذنك.
