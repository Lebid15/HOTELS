# إعدادات الرفع على السيرفر

هذا الملف مخصص لحفظ إعدادات النشر الفعلية (تُملأ عند النشر — **لا تُرفَع أسرار حقيقية إلى Git**).

> 📎 **المرجع الكامل للنشر:** `DEPLOY.md` (الخطوات + الأوامر + Reverse Proxy + النسخ الاحتياطي)، ونموذج المتغيّرات في `backend/.env.example` و`frontend/.env.example`، وقائمة الإطلاق في `RELEASE_CHECKLIST.md`.

- قاعدة البيانات: PostgreSQL
- اسم المستخدم:
- كلمة المرور:
- مضيف السيرفر:
- منفذ السيرفر:
- إعدادات البيئة (ENV):
- عنوان الواجهة الأمامية:
- عنوان الواجهة الخلفية:

> لا تملأ هذا الملف الآن إلا بعد أن نتفق على إعدادات النشر النهائية.