# اختبارات funduqii

## التشغيل
```bash
cd backend
python manage.py test api
```

> **العدّ المُثبَّت (المرحلة 11):** `python manage.py test api` → **208 اختبارًا، كلّها ناجحة** (0 فشل/0 خطأ، ~292s) — **152** من مراحل الإغلاق التجاري + **56** جديدة لسلسلة التحصين (`api/tests_hardening.py`، انظر §ب). آخر تشغيل: 2026‑07‑02.

---

## أ) شبكة الأمان الحالية (مراحل الإغلاق التجاري — مُنفَّذة)

| المجموعة | يغطّي | أمثلة |
|---|---|---|
| `TenantIsolationTests` | عزل المستأجرين + B‑7 | المدير لا يرى/يعدّل/يحذف موارد فندق آخر · `hotel` للقراءة فقط · رفض الإنشاء بفندق آخر |
| `VerticalRBACTests` | B‑8 | الاستقبال لا يُنشئ Staff/Room/طلب اشتراك ولا يحذف حجزًا · المدير يُنشئ Staff |
| `PlatformScopeTests` | صلاحيات المنصّة | المدير محظور من `/platform/*` · المجهول محظور · المالك يرى كل الفنادق |
| `SuspendedHotelTests` | H‑5 | الفندق الموقوف يحجب موظفيه · الفعّال يسمح |
| `TokenBlacklistTests` | B‑3 | الخروج يُبطل refresh |
| `DocPrivacyTests` | B‑6 | القائمة بلا صور وثائق · التعديل الجزئي لا يمسح الوثائق |
| `PublicBookingTests` | B‑1 | رقم حجز عشوائي · منع الحجز المزدوج · تقنيع البريد/الهاتف |
| `FinancialVoidTests` | C1 | إبطال دفعة/مصروف بسبب + استثناء المبطل من الإجمالي |
| `PublicVisibilityTests` | C3 | شروط ظهور الفندق العام خادميًّا |
| `GranularPermissionTests` | C6 | إلزام صلاحية القسم للموظّف |
| `CommissionTests` | العمولة | الحجز العام يُنشئ سجل عمولة |

> ملاحظة: تُنظَّف الـcache في `setUp` لتفادي تراكم عدّادات الـthrottle بين الاختبارات.

---

## ب) اختبارات سلسلة التحصين 1–8 (✅ مُنفَّذة — `api/tests_hardening.py`، 56 اختبارًا ناجحًا)

> نُفِّذت كلّها وشُغِّلت في المرحلة 11 (كلّها خضراء). القوائم أدناه خريطة التغطية.

### 1) FolioCharge (المرحلة 1)
- [ ] إنشاء FolioCharge.
- [ ] إبطال FolioCharge بسبب → ينجح.
- [ ] رفض الإبطال بدون سبب (400).
- [ ] رفض الإبطال المكرّر (400).
- [ ] منع تعديل FolioCharge مبطل (400).
- [ ] استثناء المبطل من `balance_due` / الرصيد المستحق.
- [ ] تسجيل حدث `folio_charge.void` في Audit Log.
- [ ] منع `DELETE` (405 — لا مسار حذف).

### 2) بوّابة الحجز العام (المرحلة 2)
- [ ] فندق مخفي (`public_listing_enabled=false`) لا يظهر ولا يقبل حجزًا.
- [ ] فندق بلا قبول اتفاقية فعّالة لا يقبل حجزًا.
- [ ] فندق باشتراك منتهٍ لا يقبل حجزًا.
- [ ] باقة لا تسمح بالظهور → لا يظهر.
- [ ] باقة لا تسمح بالحجز → لا يقبل حجزًا.
- [ ] غرفة غير ظاهرة (`show_in_public=false`) لا تظهر في التوفّر.
- [ ] تجاوز `hotel_id` مباشرةً لفندق غير مؤهّل → يُرفَض (400).
- [ ] `availability` لفندق لا يقبل الحجز → قائمة فارغة.

### 3) manage_token (المرحلة 3)
- [ ] إنشاء حجز عام يُرجع `manage_token`.
- [ ] إنشاء حجز عام يُرجع `manage_url`.
- [ ] lookup برقم + هاتف **لا** يُرجع `manage_token`.
- [ ] lookup برقم + هاتف **لا** يُرجع `manage_url`.
- [ ] lookup برقم + token **لا** يُرجع `manage_token`.
- [ ] إلغاء حجز عبر `token` ينجح.
- [ ] رسالة الفشل عامّة (لا تكشف أيّ الحقلين خاطئ).

### 4) Payment (المرحلة 4)
- [ ] دفعة مفردة صحيحة (amount + method).
- [ ] دفعة مقسّمة صحيحة (المجموع = amount).
- [ ] رفض مجموع أقل من amount.
- [ ] رفض مجموع أكبر من amount.
- [ ] رفض قيمة سالبة.
- [ ] رفض `amount = 0`.
- [ ] `settle_and_checkout` يُنشئ دفعة متّسقة (amount = مجموع الأجزاء).
- [ ] الدفعة المبطلة لا تدخل في الإجمالي.

### 5) تطبيع الهاتف (المرحلة 5)
- [ ] حجز محفوظ بصيغة يُعثر عليه بصيغة أخرى (+/00/0).
- [ ] أرقام بمسافات/شرطات تعمل.
- [ ] أرقام عربية/فارسية تُحوَّل وتُطابِق.
- [ ] رقم خاطئ لا يُرجع حجزًا.
- [ ] Public lookup **لا** يكشف `guest_phone_normalized`.
- [ ] Public lookup يُبقي الهاتف **مقنّعًا**.
- [ ] مسار `manage_token` لم يتأثّر.
- [ ] الـbackfill يعمل على البيانات القديمة.

### 6) الصلاحيات (المرحلة 6)
- [ ] مستخدم بلا `UserProfile` → 403 من قسم محميّ.
- [ ] `username=manager` بلا Profile → لا دور.
- [ ] `username=platform` بلا Profile → لا دور.
- [ ] `username=reception` بلا Profile → لا دور.
- [ ] `UserProfile.role=manager` يعمل.
- [ ] `UserProfile.role=reception` يعمل.
- [ ] `UserProfile.role=platform_owner` يعمل.

### 7) Throttling (المرحلة 7)
- [ ] `availability` → 429 بعد تجاوز الحد (public_availability 30/min).
- [ ] `public booking` → 429 بعد تجاوز الحد (5/min).
- [ ] `lookup/manage` → 429 بعد تجاوز الحد (20/min).
- [ ] `cancel` → 429 بعد تجاوز الحد (10/min).
- [ ] `review` → 429 بعد تجاوز الحد (5/min).
- [ ] الاستخدام الطبيعي (عدد معقول/دقيقة) لا يتأثّر.

### 8) FoodOrder (المرحلة 8)
- [ ] طلب بدفع نقدي كامل.
- [ ] طلب بدفع إلكتروني كامل.
- [ ] طلب ببطاقة كامل.
- [ ] طلب على حساب الغرفة (`amount_room`).
- [ ] طلب مقسّم صحيح (المجموع = amount).
- [ ] رفض مجموع أقل من الإجمالي.
- [ ] رفض مجموع أكبر من الإجمالي.
- [ ] رفض قيمة سالبة.
- [ ] تعديل الأصناف بلا إرسال توزيع لا يكسر الطلب.
- [ ] PATCH بتوزيع خاطئ يُرفَض.
- [ ] التقارير (تجمع على `amount`/`payment_method`) لا تتأثّر.

---

## ج) الواجهة (تُشغَّل في المرحلة 11)
```bash
cd frontend
npm run lint        # exit 0
npm run typecheck   # نظيف
npm run build       # ناجح (hermetic — خط محلّي)
```

## د) فحص الإنتاج (المرحلة 9 — مُثبَت)
```bash
DJANGO_DEBUG=false DJANGO_SECRET_KEY="<long>" DJANGO_ALLOWED_HOSTS="..." \
CORS_ALLOWED_ORIGINS="..." CSRF_TRUSTED_ORIGINS="..." DATABASE_URL="postgres://..." \
python manage.py check --deploy      # النتيجة: 0 issues
```

## هـ) نتائج المرحلة 11 (آخر تشغيل 2026‑07‑02)

| الفحص | الأمر | النتيجة |
|-------|-------|---------|
| اختبارات الخلفية | `python manage.py test api` | **208 ناجحة · 0 فشل · 0 خطأ** (~292s) |
| فحص النظام | `python manage.py check` | 0 مشاكل |
| الترحيلات | `makemigrations --check --dry-run` | لا معلّقات (أحدث 0038) |
| فحص الإنتاج | `check --deploy` (env إنتاجيّ) | **0 issues** |
| Lint الواجهة | `npm run lint` | exit 0 |
| فحص الأنواع | `npm run typecheck` | 0 أخطاء |
| بناء الواجهة | `npm run build` | ✓ Compiled successfully (46/46 صفحة) |
| تدقيق الحزم | `npm audit` | **ثغرتان متوسّطتان** (postcss عبر Next) — **مؤجّلتان** |

**مؤجّل موثَّق:** ثغرتا `postcss` (moderate: XSS في CSS Stringify) تابعتان لـNext؛ إصلاحهما يتطلّب `npm audit fix --force` الذي **يُنزِل Next 16 → 9.3.3 (كسر كامل)** — غير مقبول؛ يُعالَج ضمن ترقية تبعيات مخطّطة. لا ثغرات عالية/حرجة.

**إصلاحات المرحلة 11:** لم يظهر خلل كود؛ الإخفاقات الوحيدة (8) كانت في fixtures اختباريّة قديمة (`PublicBookingTests`/`CommissionTests`) لم تُفعِّل `public_listing_enabled`/`allow_public_listing` — وهو سلوك م2 المقصود (الحجز يتطلّب كل شروط الظهور) — فأُصلحت الـfixtures فقط (لا تغيير كود تطبيقي).
