# اختبارات funduqii

## التشغيل
```bash
cd backend
python manage.py test api
```

## التغطية (29 اختبارًا — شبكة أمان القبول، B‑5)

| المجموعة | يغطّي | أمثلة |
|---|---|---|
| `TenantIsolationTests` | عزل المستأجرين + B‑7 | المدير لا يرى/يعدّل/يحذف موارد فندق آخر · `hotel` للقراءة فقط (PATCH لا ينقل الحجز) · رفض الإنشاء بفندق آخر |
| `VerticalRBACTests` | B‑8 | الاستقبال لا يُنشئ Staff/Room/طلب اشتراك ولا يحذف حجزًا · يُنشئ/يعدّل الحجوزات · المدير يُنشئ Staff |
| `PlatformScopeTests` | صلاحيات المنصّة | المدير محظور من `/platform/*` وإنشاء الباقات · المجهول محظور · المالك يرى كل الفنادق |
| `SuspendedHotelTests` | H‑5 | الفندق الموقوف يحجب موظفيه · الفعّال يسمح |
| `TokenBlacklistTests` | B‑3 | الخروج يُبطل refresh (رفض بعده) |
| `DocPrivacyTests` | B‑6 | القائمة بلا صور وثائق · التفصيل يشملها · **التعديل الجزئي لا يمسح الوثائق** |
| `PublicBookingTests` | B‑1 | رقم حجز عشوائي · منع الحجز المزدوج · رفض التواريخ الخاطئة · تقنيع البريد/الهاتف في الاستعلام |
| `CommissionTests` | العمولة | الحجز العام يُنشئ سجل عمولة |

> ملاحظة: تُنظَّف ذاكرة الـcache في `setUp` لتفادي تراكم عدّادات الـthrottle بين الاختبارات.
