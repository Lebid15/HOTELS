// Shared app-wide AR→EN dictionary. The physical entries currently live in the
// manager translations file (historical); re-exported here as the single source
// so all roles import from "@/lib/i18n". New reception/platform strings are added
// to the same EN map.
export { EN } from "@/app/manager/translations";
